# OpenAI 设备码登录第七阶段交付验收报告

## 1. 文档状态

- 文档性质：Phase 7 正式交付验收工作文档
- 编制日期：2026-07-30
- 当前功能分支：`feature/openai-device-code-login`
- 当前结论：Phase 0～Phase 7 已通过；功能分支已通过个人 PR #1 合入默认 `main`
- Phase 6 状态：**已通过**
- Phase 7 状态：**已通过**

> Phase 6 的真机证据来自用户本人反馈，本文件不能把未执行真机场景写成通过。
> Phase 7 只有在所有阻断缺陷闭环，并完成最终差异、文档、Git 和个人远端核对后
> 才能正式通过。

## 2. 本次交付范围

本次交付面向个人 Fork 中的 Android OpenAI 设备码登录功能，主要包括：

1. OpenAI OAuth 日志与凭据清理安全基础；
2. 设备授权协议模型、HTTP 请求和轮询结果分类；
3. 登录状态协调、取消、超时、重试和迟到结果保护；
4. 新建 OpenAI Provider 时的浏览器登录与设备码登录双入口；
5. 严格的 Provider、凭据和模型跨存储提交、补偿及启动恢复；
6. 设备码 Token 与既有 OpenAI OAuth 自动刷新路径的兼容回归；
7. 已由用户安装并完成核心 Phase 6 验收的 Debug APK；
8. 个人 Fork、官方只读上游、对应源码和后续同步维护说明；
9. 根目录个人 `README.md` 与重命名保留的上游官方项目说明；
10. 个人 Fork 相对官方上游的逐文件修改清单。

以下内容明确不属于“已完成真机验证或生产交付”的范围：

- 同签名覆盖升级；
- 取消、断网恢复、后台、锁屏、旋转、Activity 重建、进程死亡和删除后新建；
- 真实 Token 自然到期后的自动刷新；
- 旧浏览器登录、其他 Provider、模型加载失败和恢复的完整真机回归；
- Release 签名、应用商店分发或生产发布；
- 向官方仓库创建 Pull Request；
- Phase 7 尚未完成的最终提交、推送和远端一致性门禁。

## 3. Phase 0～Phase 6 提交与证据映射

下表中的短 SHA 是各 Phase 的主要代码或验收提交入口。需要审计时应使用
`git show <SHA>` 查看完整提交，并结合对应报告核对测试边界。

| Phase | 主要提交 | 主要内容 | 当前交付含义 |
|---|---|---|---|
| Phase 0 | `9f64e60` | 基线环境、分支、协议复核与已有失败记录 | 建立后续阶段的可比较基线 |
| Phase 1 | `3ba1e1e` | 凭据清理、敏感日志收口与相关安全回归 | 建立设备码接入前的安全边界 |
| Phase 2 | `b42f3fb` | 设备授权协议模型、单次 HTTP 请求和响应分类 | 提供可独立测试的协议核心 |
| Phase 3 | `b27fc9e` | 登录状态机、协调器、取消、超时、重试和竞态保护 | 提供路由级生命周期与并发基础 |
| Phase 4 | `60fa227` | UI 接入、Provider 持久化、补偿、恢复和模型集成 | 完成面向用户的功能闭环 |
| Phase 5 | `a000037` | 设备码 Token 自动刷新兼容与回归测试 | 形成此次 Debug APK 的代码状态 |
| Phase 6 | 用户真机反馈及本报告所在提交 | 安装、设备码授权、保存、重启、模型加载和最小真实请求 | 用户接受核心真机版本，未执行项继续保留 |

以上提交均位于 `feature/openai-device-code-login` 的连续历史中。正式交付前仍须
重新执行以下检查，防止后续提交、上游同步或缺陷修复使映射失效：

```powershell
git log --oneline --decorate -10
git merge-base --is-ancestor 9f64e60 HEAD
git merge-base --is-ancestor a000037 HEAD
```

## 4. 构建提交 A、文档提交 B 与远端 HEAD

必须区分以下三个身份，不能把“最新文档提交”误写成 APK 的构建源码：

### 4.1 构建提交 A

- 短 SHA：`a000037`
- 完整 SHA：`a0000370cf15027c40437ad06140a03260465827`
- 提交说明：`认证：完成设备码令牌 Phase 5 自动刷新回归`
- 作用：本报告所列 Debug APK 的实际构建源码

APK 应始终对应构建提交 A。后续只修改 `yangyunzhao-fork/` 文档的提交，不会改变已经生成的
APK 字节，也不能被写成 APK 的“对应 Git 提交”。

### 4.2 文档与交付提交

- Phase 5/6/7 预备材料提交：
  `327bfe24b3932a3099fa482cccde0316b26e9538`
- 中文文件名整理提交：
  `86be05d8e10e65fe89110e3e243b90ad61073680`
- Phase 6 结果、个人 README、修改清单和 Phase 7 主要交付提交：
  `e75f016aa161c54c352cba14cf566649d768f037`
- 最终状态收口提交：以本报告所在的个人远端功能分支 HEAD 为准，不在提交形成前
  编造自引用 SHA
- 作用：保存验证证据、个人仓库说明和交接材料
- 限制：这些纯文档提交不是 APK 的构建提交

最终状态从 Git 读取：

```powershell
git log -1 --format='%H %s'
```

### 4.3 个人远端功能分支 HEAD

- Phase 7 开始时已确认的远端 HEAD：
  `86be05d8e10e65fe89110e3e243b90ad61073680`
- Phase 7 主要交付提交推送后的远端 HEAD：
  `e75f016aa161c54c352cba14cf566649d768f037`
- Phase 7 最终状态提交：
  `8078c01efc2267b5e7c2abb232a7239ad9a27bcc`
- 个人 PR #1 合并提交：
  `5482735474d932bcd971b59a017ef570700f21a4`

核对命令：

```powershell
git fetch origin
git rev-parse HEAD
git rev-parse origin/feature/openai-device-code-login
git status --short --branch
```

若本地 HEAD 与个人远端功能分支 HEAD 不一致，应先确认差异原因；不得仅为“看起来
一致”而使用破坏性 reset、普通 `--force` 或丢弃未提交工作。

## 5. Debug APK 身份

| 项目 | 值 |
|---|---|
| 文件名 | `app-debug.apk` |
| 仓库相对路径 | `src/android/app/build/outputs/apk/debug/app-debug.apk` |
| 本机绝对路径 | `D:\repositories\OpenMinis\src\android\app\build\outputs\apk\debug\app-debug.apk` |
| 文件大小 | `40,716,639` 字节 |
| SHA-256 | `BA70EE63B3E7D2E3F3F992053A6A908010621464370817DE8F4081980CEEC931` |
| 对应构建提交 | `a0000370cf15027c40437ad06140a03260465827` |
| 构建类型 | `Debug` |
| 用途 | 已用于 Phase 6 用户真机验收；仍是个人 Debug 构建，不是官方或 Release 包 |

用户安装前应在本机重新计算哈希：

```powershell
Get-FileHash `
  -Algorithm SHA256 `
  -LiteralPath 'D:\repositories\OpenMinis\src\android\app\build\outputs\apk\debug\app-debug.apk'
```

若文件大小或 SHA-256 与本表不一致，应停止安装。此时应确认是否重新构建过 APK，
并以新的构建提交、文件大小和哈希更新 Phase 6 清单及本文件，不能继续沿用旧身份。

## 6. 报告与操作入口

### 6.1 需求、计划和持续记录

- [需求与已确认决策](../需求与决策.md)
- [Phase 0～Phase 7 实施计划](../分阶段实施计划.md)
- [持续工作、非阻塞问题与恢复上下文](../持续工作记录.md)

### 6.2 分阶段报告

- [Phase 0 基线报告](第零阶段基线验收报告.md)
- [Phase 1 安全基础报告](第一阶段安全基础验收报告.md)
- [Phase 2 协议核心报告](第二阶段协议核心验收报告.md)
- [Phase 3 状态协调报告](第三阶段状态协调验收报告.md)
- [Phase 4 UI 与持久化集成报告](第四阶段界面与持久化集成验收报告.md)
- [Phase 5 自动化回归与 Debug 构建报告](第五阶段自动化回归与调试构建验收报告.md)

### 6.3 下一步与维护入口

- [Phase 6 真机验收清单](第六阶段真机验收清单.md)
- [个人 Fork 与官方上游同步约定](../../../仓库维护/个人派生仓库与官方上游同步说明.md)
- [个人 Fork 相对官方上游修改清单](../../../仓库维护/个人派生仓库相对官方上游修改清单.md)
- [Windows 上游同步脚本](../../../仓库维护/Sync-Upstream.ps1)
- [个人仓库首页](../../../../README.md)
- [重命名保留的上游官方项目说明](../../../../上游官方项目说明.md)

个人 `README.md` 已通过个人仓库 PR #1 合入默认 `main`，现在是 GitHub 个人仓库
默认首页。功能分支暂时保留，便于追溯和后续维护。

## 7. Git 远端与安全边界

### 7.1 远端角色

| 名称 | 地址或配置 | 用途 |
|---|---|---|
| `origin` | `https://github.com/yangyunzhao/OpenMinis.git` | 保存个人分支、个人修改和交付历史 |
| `upstream` fetch | `https://github.com/OpenMinis/OpenMinis.git` | 获取官方公开镜像更新 |
| `upstream` push | `DISABLED` | 防止误向官方上游推送 |

安全边界：

1. 功能分支只推送到个人远端 `origin`；
2. `upstream` 只读，不向其推送；
3. 功能分支合入个人 `main` 前必须由用户明确决定并完成相应验证；本轮已通过个人
   PR #1 满足该条件；
4. 不使用 `git push --force`；确需在个人独占分支 rebase 后更新时也只能审慎使用
   `--force-with-lease`；
5. 上游冲突必须理解双方修改意图并运行相关测试，不能自动选择 `ours` 或
   `theirs`；
6. 同步前检查工作区，避免把用户或其他任务的未提交修改混入同步过程。

### 7.2 上游同步入口

完整说明以 [个人派生仓库与官方上游同步说明](../../../仓库维护/个人派生仓库与官方上游同步说明.md) 为准。日常流程为：

```powershell
git switch main
.\yangyunzhao-fork\仓库维护\Sync-Upstream.ps1
git switch feature/openai-device-code-login
git rebase main
```

若功能分支不适合改写历史，可使用 `git merge main`。发生冲突时脚本会保留现场，
应先检查：

```powershell
git status
git diff --name-only --diff-filter=U
```

合并或 rebase 完成后，需要重新执行与冲突文件相关的 focused tests、完整 JVM
回归、静态检查和 Debug 构建；如果产生新 APK，还必须更新 APK 身份和 Phase 6
清单。

## 8. 官方 PR 状态

- 尚未向 `OpenMinis/OpenMinis` 创建 Pull Request。
- 本阶段也不计划自动创建官方 Pull Request。
- 官方仓库在现有项目说明中属于不接受 PR 的公开镜像；功能建议应按其说明通过
  Issue 等渠道提出。
- 用户最初设想的“由原作者决定是否合并”不能被当作已经发生；若官方政策未来变化，
  仍须由用户确认是否公开提交代码、说明和测试证据。

个人仓库状态：

- [个人 PR #1](https://github.com/yangyunzhao/OpenMinis/pull/1) 已使用 merge
  commit 合入个人默认 `main`；
- 合并仅发生在 `yangyunzhao/OpenMinis`，不涉及官方镜像；
- 功能分支暂时保留，尚未自动删除。

任何未来的公开 Issue、PR 或发布说明都不得包含真实授权码、Token、设备授权 ID、
PKCE、账号标识、私有代理信息或未经脱敏的日志。

## 9. GPLv3 与对应源码

OpenMinis 使用 GPLv3。个人可以 fork、修改和自行构建；若把修改后的 APK
提供给他人，必须同时履行 GPLv3 下的义务，尤其需要注意：

1. 为所分发的二进制提供其对应版本的完整对应源码；
2. 保留版权和许可证声明；
3. 修改后的派生作品继续按 GPLv3 提供；
4. 确保接收者能获得用于生成该二进制的实际源码，而不是仅获得更新后的文档 HEAD。

因此，本 APK 的对应源码锚点是构建提交 A
`a0000370cf15027c40437ad06140a03260465827`。如果文档提交 B 成为远端 HEAD，
对外说明时仍必须明确 APK 是由构建提交 A 生成；若后来修复代码并重新构建，则应建立
新的构建提交、APK 哈希和对应源码关系。

本报告不是法律意见。计划对外分发时，应重新核对仓库中的实际许可证文本、依赖
许可证和分发方式。

## 10. Phase 6 实际结果与剩余风险

### 10.1 安装与签名

- 当前产物是 Debug APK，不是 Release 包。
- Debug APK 使用本机构建环境的调试签名；若设备上已有相同包名但签名不同的应用，
  可能无法覆盖安装。
- 不应为解决签名冲突而直接卸载日常使用的应用，因为卸载可能清除 Provider、会话和
  其他本地数据。
- 遇到安装失败时先保存错误信息并停止，由用户决定是否使用备用设备、备份数据或采用
  其他隔离方案。
- 本次真机确实遇到签名不同导致覆盖安装失败；用户确认旧应用没有数据并主动卸载后，
  全新安装成功。
- 本轮没有验证相同签名条件下的覆盖升级，不能宣称升级兼容性通过。

### 10.2 真实协议

- OpenAI/Codex 设备授权端点和响应行为不是本项目可以保证长期稳定的公开集成契约。
- MockWebServer 和 JVM 测试只能验证当前实现对已知响应的处理，不能证明真实服务端
  没有改变端点、字段、轮询状态、限流或错误语义。
- 真实页面的域名、证书、提示内容或请求看起来异常时应立即停止，不输入授权码。
- 一次性授权码同样属于敏感信息，不应出现在聊天、截图、Issue、报告或公开日志中。
- 本次用户已通过官方页面完成设备授权，返回应用后显示认证完成；这证明当时的真实
  主链路可用，但不构成未来服务端兼容承诺。

### 10.3 真实请求与账号影响

- 真实登录会使用用户有权使用的 ChatGPT/OpenAI 账号，并可能改变该账号的授权状态。
- 真实模型请求可能消耗额度、配额或产生费用。
- 用户已在 Phase 6 明确允许一次最小真实请求，使用
  `GPT-5.3 Codex Spark` 成功收到 `OK`；该结果不代表其他模型或长期可用性。
- Token 自动刷新不要求通过篡改系统时间、加密存储或账号状态强行触发。
- 本轮没有等待真实 Token 自然到期；自动刷新只有 Phase 5 自动化证据，真机结果
  继续待长期观察。
- 不使用日常关键 Provider 做破坏性验收；“已有 Provider 重新登录”的产品决策是
  删除旧 Provider 后重新新建。

### 10.4 Android 生命周期与本地存储

以下行为本轮没有完成真机验证，不能用 JVM 测试、Debug 构建成功或 Phase 6
总体通过替代：

- Activity 被系统回收后重建；
- 旋转、切后台、锁屏、返回应用和取消授权；
- 浏览器或 Custom Tab 中断后回到应用；
- 进程强杀发生在跨存储提交的不同阶段；
- Android Keystore、EncryptedSharedPreferences 和 Room 的真实组合行为；
- 删除新建 Provider 后，凭据、模型和恢复标记是否按预期清理；
- 厂商后台限制或深度睡眠对 15 分钟超时和轮询的影响。

## 11. Phase 6 用户验收摘要

用户已于 2026-07-30 明确接受 Phase 6 并允许进入 Phase 7。已验证：

1. APK SHA-256 与交付记录一致；
2. 用户确认旧应用无数据后，全新安装成功并正常启动；
3. API Key、OAuth、设备码和浏览器回调入口可见；
4. 设备码请求、网址与授权码展示、复制反馈和系统浏览器打开正常；
5. 官方页面授权成功，应用返回“已认证”状态；
6. 用户明确点击“保存AI服务商”后列表只出现一个 Provider；
7. 应用从最近任务移除并重启后 Provider 和登录状态仍保留；
8. 首页可见 12 个模型；
9. 用户明确接受可能用量后，最小真实请求成功收到 `OK`；
10. 已保存 Provider 详情页没有设备码重新授权入口。

用户明确选择不再为取消、网络异常等低收益场景重复登录。完整的“通过、部分通过、
未执行”矩阵见
[Phase 6 真机验收清单](第六阶段真机验收清单.md)。
逻辑级自动化对部分异常路径提供证据，但不能替代未执行的真机场景。

## 12. 缺陷回流规则

Phase 6 发现问题后，不在本文件中直接把 Phase 7 标记为通过，也不把所有问题都归为
“真机差异”。应按根因回到责任 Phase：

| 缺陷类型 | 主要回流 Phase | 修复后的最低复验 |
|---|---|---|
| 日志泄露、删除后凭据残留 | Phase 1 | 安全 focused tests、完整 JVM 回归、真机相关项 |
| 端点、字段、轮询状态或 Token 交换变化 | Phase 2 | 协议测试、错误分类、完整 JVM 回归、真机登录 |
| 超时、取消、重试、迟到结果或并发竞态 | Phase 3 | 协程/并发测试、生命周期真机项 |
| UI、复制、Custom Tab、Provider 提交、补偿或恢复 | Phase 4 | UI/持久化 focused tests、Debug 构建、真机对应项 |
| 自动刷新、兼容回归或构建产物 | Phase 5 | 刷新测试、完整 JVM 回归、Lint 基线比较、重新构建 |

缺陷闭环步骤：

1. 在工作记录中新增编号、现象、影响和是否阻塞；
2. 保存脱敏后的最小复现信息；
3. 在责任 Phase 做范围最小且完整的修复；
4. 先执行 focused tests，再执行后续阶段门禁；
5. 代码变化后创建新的详细中文提交；
6. 重新生成 APK 并更新大小、SHA-256 和构建提交；
7. 只重测受影响项不足以证明整体无回归时，补做完整 Phase 6；
8. 用户明确接受新的真机结果后，再更新正式 Phase 7 结论。

如问题不阻塞当前验收但需要后续观察，应记录到
[持续工作记录](../持续工作记录.md)，不能依赖聊天上下文保存。

## 13. Phase 7 正式验收门禁

以下项目全部满足前，Phase 7 必须保持“未通过”：

- [x] 用户已完成并接受 Phase 6 真机验收。
- [x] Phase 6 没有发现阻断缺陷；D6-01/D6-02 是用户接受的极低优先级建议。
- [x] 最终构建提交、APK 大小和 SHA-256 已重新核对。
- [x] Phase 7 主要交付提交和最终状态提交已推送到个人远端。
- [x] 本地 HEAD 与 `origin/feature/openai-device-code-login` 一致。
- [x] 最终工作区干净，没有意外修改或未跟踪文件。
- [x] `origin` 和只读 `upstream` 的角色与 push 防护仍正确。
- [x] 未向官方仓库推送，也没有向官方镜像创建 Pull Request。
- [x] 自动化测试、已有基线失败、未执行验证和真机结果均如实记录。
- [x] 对应源码已随个人功能分支提供，根 README 已记录 GPLv3 与许可证边界；当前未宣称官方或生产发布。
- [x] 本次交付基于已获取的
  `upstream/main@9cf3a855fecd27bb5735b84cacbd56852a3ab8dd`，该提交是当前功能分支共同基线。

正式通过时，应在本文件或独立的最终 Phase 7 报告中补充：

1. Phase 6 用户接受日期与不含敏感信息的结论；
2. 最终构建提交；
3. 最终文档提交；
4. 个人远端功能分支 HEAD；
5. APK 最终大小和 SHA-256；
6. 自动化与真机测试摘要；
7. 已知风险、跳过项和用户决定；
8. 已通过个人 PR #1 合并个人 `main`，功能分支暂时保留；是否公开分发另行决定。

最终结论：

> **Phase 7 已通过：功能分支、个人 README、上游 README 副本、修改文件清单、
> 验收证据和维护说明均已推送到个人远端，并通过个人 PR #1 合入默认 `main`。
> 官方镜像没有收到推送或 PR。**

## 14. Phase 7 已执行验证

| 检查 | 实际结果 |
|---|---|
| 上游刷新与共同基线 | `git fetch upstream main` 成功；共同基线为 `9cf3a855fecd27bb5735b84cacbd56852a3ab8dd` |
| Fork 文件范围 | 相对官方上游含工作区新增文件共 39 个：根目录说明 2、生产代码与资源 14、测试 9、脚本 1、`yangyunzhao-fork` 文档 13 |
| README 官方副本 | `上游官方项目说明.md` 与该上游提交的 `README.md` 内容一致 |
| 聚焦 Markdown 链接 | 根目录两份说明与 `yangyunzhao-fork` 的 13 份 Markdown，共 15 个文件的本地相对链接全部存在 |
| 全仓库 Markdown 探测 | 发现 12 个上游或子模块既有特殊链接，包括缺失的子模块许可证副本、站点根路径和 `minis://` 协议；不属于本次回归 |
| 产品源码身份 | `a0000370cf15027c40437ad06140a03260465827` 之后 `src/android/app/src` 没有变化 |
| APK 身份 | `40,716,639` 字节；SHA-256 为 `BA70EE63B3E7D2E3F3F992053A6A908010621464370817DE8F4081980CEEC931` |
| PowerShell 语法 | `yangyunzhao-fork/仓库维护/Sync-Upstream.ps1` 通过 PowerShell AST 解析 |
| 敏感凭据模式扫描 | 新 README、上游说明和 `yangyunzhao-fork` 未匹配常见真实 Token/Authorization 凭据模式 |
| Git 空白检查 | `git diff --cached --check` 通过；仅出现 Windows 工作区预期的 LF→CRLF 提示 |

本阶段没有修改产品源码，因此没有重复运行 Phase 5 的 Android JVM 全量回归、
Lint 或 Debug 构建；产品验证继续引用 Phase 5 结果和构建提交，Phase 7 只执行与
文档、Git、APK 身份、安全和远端范围相匹配的检查。
