# OpenAI 设备码登录 Phase 6 真机验收清单

## 1. 文档状态与使用边界

- 文档状态：待用户执行
- Phase 5 状态：已通过，报告见
  [OPENAI_DEVICE_CODE_LOGIN_PHASE5_REPORT.md](OPENAI_DEVICE_CODE_LOGIN_PHASE5_REPORT.md)
- Phase 6 状态：待验收
- Phase 7 状态：交付材料已预备，正式验收待 Phase 6 通过
- 编制日期：2026-07-30
- 适用功能：Android 新增官方 OpenAI Provider 时的设备码登录
- 自动化边界：本清单的编制过程没有启动 Android 模拟器、没有操作真实设备、没有使用真实 ChatGPT/OpenAI 账号，也没有发起真实模型请求

本清单由用户在真实 Android 设备上逐项执行。Codex 只在用户明确同意后协助分析用户主动提供的脱敏现象或日志，不远程控制真机完成登录。

只有 Phase 5 已通过并提供可验证的 Debug APK 后，才能开始本清单。当前没有真实执行证据的项目必须保持“未执行”，不得预先填写为通过。

状态填写约定：

- `[ ]`：未执行
- `[x]`：已执行且符合预期
- `[!]`：已执行但不符合预期
- `[-]`：经用户确认，本轮不执行

## 2. 待验版本身份

开始安装前，由 Codex 填写并由用户复核：

| 项目 | 值 |
|---|---|
| APK 文件名 | `app-debug.apk` |
| APK 本地路径 | `D:\repositories\OpenMinis\src\android\app\build\outputs\apk\debug\app-debug.apk` |
| APK 大小 | `40,716,639` 字节 |
| APK SHA-256 | `BA70EE63B3E7D2E3F3F992053A6A908010621464370817DE8F4081980CEEC931` |
| 对应 Git 提交 | `a0000370cf15027c40437ad06140a03260465827`（Phase 5 产品/构建提交 A） |
| 功能分支 | `feature/openai-device-code-login` |
| 个人远端 | `origin` / `yangyunzhao/OpenMinis` |
| 构建类型 | `Debug` |
| 构建日期 | `2026-07-30 05:37:23 +08:00` |

该默认输出路径会被后续 `assembleDebug` 覆盖。若 APK 被重新构建，即使文件名没有变化，
也必须重新计算大小和 SHA-256，并更新本表；不得继续使用旧校验值。

### 2.1 安装前校验

- [x] APK 文件名、提交和 SHA-256 已由 Codex 填写。
- [ ] 用户在本机重新计算 SHA-256，结果与上表完全一致。
- [ ] 用户确认 APK 来源是自己的功能分支，不是聊天附件或未知第三方重新打包版本。
- [ ] APK 对应提交可以在本地仓库中找到。
- [ ] 如任一信息不一致，停止安装并记录到第 12 节。

Windows PowerShell 校验示例：

```powershell
Get-FileHash -Algorithm SHA256 -LiteralPath '<APK 的完整路径>'
```

实际计算结果：

```text
<用户填写>
```

## 3. 安全提示与用户确认

### 3.1 必须了解

- 一次性授权码、设备授权 ID、authorization code、PKCE、Access Token、Refresh Token 和 ID Token 都不得粘贴到本文件、聊天、Issue、截图说明或公开日志中。
- 授权页面只应来自应用展示的官方登录网址。若域名、证书、页面内容或授权请求看起来异常，立即停止。
- 不要在共享屏幕、公开录屏或旁人可见环境中展示授权码。
- 不要为了测试而删除日常使用的 Provider、应用数据或账号授权。
- 不要卸载现有应用来“解决”升级失败；卸载可能清除应用数据，应先停止并分析签名、版本或安装错误。
- 真实模型请求可能产生账户用量、配额消耗或费用。只有用户明确同意后才能执行第 9 节。
- Token 自动刷新不要求为了验收而等待 Token 真实到期，也不应通过篡改系统时间、账号或加密存储强行触发。

### 3.2 用户确认

- [ ] 我确认使用的是我有权使用的测试账号或个人账号。
- [ ] 我确认当前设备和网络由我控制。
- [ ] 我理解一次性授权码也属于敏感信息，不会把它写入反馈材料。
- [ ] 我确认不会删除日常使用的 Provider 或应用数据。
- [ ] 我已决定本轮是否允许真实模型请求：
  - [ ] 允许执行一次最小真实请求，接受可能产生的账户用量
  - [ ] 本轮不执行真实模型请求
- [ ] 我已决定本轮是否抽查已有 Provider：
  - [ ] 只检查入口和列表，不调用已有账号
  - [ ] 允许对我指定的测试 Provider 做兼容性抽查

## 4. 设备与环境记录

不得填写真实账号邮箱、workspace 名称或其他可识别账号的信息。

| 项目 | 用户填写 |
|---|---|
| 手机厂商和型号 |  |
| Android 版本 |  |
| 系统语言 |  |
| 屏幕方向支持 |  |
| 网络类型（Wi-Fi/移动网络） |  |
| 是否使用 VPN/代理 |  |
| 浏览器名称和版本 |  |
| 安装前应用版本 |  |
| 安装方式（文件管理器/ADB/其他） |  |
| 测试开始时间和时区 |  |

## 5. 安装或安全升级

优先在现有应用上执行同签名升级，以验证已有 Provider 和应用数据不回归。

### 5.1 升级安装

- [ ] 记录安装前版本和必要的非敏感界面状态。
- [ ] 保留现有应用，不执行卸载、不清除数据。
- [ ] 安装第 2 节中完成 SHA-256 校验的 APK。
- [ ] 安装成功后打开应用，确认原有会话、Provider 列表和设置仍可见。
- [ ] 如出现签名不一致、版本降级、解析失败或需要卸载的提示，立即停止，不接受“先卸载再试”。

可选 ADB 安装命令仅供用户已配置并愿意使用 ADB 时参考：

```powershell
adb install -r '<APK 的完整路径>'
```

安装结果或错误摘要（不得粘贴敏感日志）：

```text
<用户填写>
```

### 5.2 全新安装

全新安装不是本轮默认要求。只有用户明确选择独立测试设备、独立系统用户或可安全清除的测试环境时才执行。

- [ ] 用户明确确认该环境没有需要保留的数据。
- [ ] 全新安装成功。
- [ ] 应用可以正常进入新增 Provider 页面。
- [ ] 本轮不执行全新安装。

## 6. 入口、范围与界面抽查

### 6.1 新增官方 OpenAI Provider

操作：

1. 打开 Provider 列表。
2. 选择新增 Provider。
3. 选择 OpenAI。
4. 进入账号登录配置。

预期：

- [ ] 能看到设备码登录入口。
- [ ] 设备码登录排在现有网页登录前，并明确标为“推荐”。
- [ ] 设备码不会在进入页面时自动开始。
- [ ] 现有浏览器回调登录入口仍然可见。
- [ ] 手动 Bearer Token 与设备码登录清楚分开。
- [ ] 界面没有展示 OAuth、PKCE、Access Token、Refresh Token 等要求普通用户理解的内部术语。

实际结果：

```text
<用户填写；不要包含账号信息>
```

### 6.2 ChatGPT 设备码功能可用性

设备码登录可能受个人账号安全设置、workspace 管理策略、区域或服务状态影响。官方设置入口和文案可能变化，因此测试时以当时用户看到的官方 ChatGPT 安全设置为准，不在本清单中写死可能过期的菜单名称。

- [ ] 开始前查看 ChatGPT 账号的安全设置，确认没有明确关闭设备码授权。
- [ ] 如果账号属于受管理 workspace，确认管理员策略允许设备码授权；不要自行绕过管理员限制。
- [ ] 只修改与设备码授权直接相关且用户理解的设置，不关闭双重验证或其他账号安全能力。
- [ ] 如果应用提示“不支持”或“未启用”，先记录普通用户可见提示，再检查安全设置或联系 workspace 管理员。
- [ ] 设置或策略调整后使用全新的登录尝试和新授权码重试。
- [ ] 如果官方界面没有对应设置，或用户不确定某项设置的影响，停止并与 Codex 讨论，不猜测操作。

测试时看到的实际设置名称或管理员结论（不要填写账号或 workspace 名称）：

```text
<用户填写>
```

### 6.3 范围限制

- [ ] 已保存 OpenAI Provider 的详情页没有新增设备码重新授权入口。
- [ ] 自定义 OpenAI 兼容地址不显示设备码入口。
- [ ] API Key 入口仍然可见且未被设备码流程替换。
- [ ] Kimi 和其他 Provider 的登录入口外观与行为没有明显变化。

## 7. 核心设备码登录场景

### 7.1 正常获取和展示

操作：

1. 点击“使用一次性代码登录”。
2. 等待应用取得登录网址和授权码。
3. 不要把页面截图直接发送给任何人。

预期：

- [ ] 点击后立即出现明确的请求中状态，重复点击不会创建多个请求。
- [ ] 页面显示非空登录网址。
- [ ] 页面原样显示非空授权码，不擅自改变大小写、连接符或字符。
- [ ] 页面包含“打开登录网页”“复制登录网址”“复制授权码”和“取消”。
- [ ] 页面有明确的等待授权提示，不是没有说明的永久加载。
- [ ] UI 不显示 Token、PKCE、完整服务器响应或异常堆栈。

### 7.2 复制反馈

- [ ] 点击复制授权码后得到明确的“已复制”反馈。
- [ ] 从本机剪贴板临时核对内容与界面授权码完全一致。
- [ ] 核对后使用普通无敏感文本覆盖剪贴板中的授权码。
- [ ] 点击复制登录网址后得到明确的“已复制”反馈。
- [ ] 从本机剪贴板临时核对网址与界面完全一致。
- [ ] 核对后使用普通无敏感文本覆盖剪贴板中的网址。

### 7.3 Custom Tab 和授权完成

操作：

1. 点击“打开登录网页”。
2. 核对浏览器显示的域名和证书状态。
3. 只有页面可信时才登录并输入授权码。
4. 在网页中完成授权，然后返回应用。

预期：

- [ ] 通过系统 Custom Tab 或系统浏览器打开，没有要求安装来源不明的应用。
- [ ] 浏览器打开失败时，应用给出普通用户可理解的提示，复制网址仍可用。
- [ ] 授权完成后应用自动停止轮询并进入完成登录状态。
- [ ] 应用没有依赖 `localhost` 回调，也没有要求用户允许手机本地端口监听。
- [ ] 应用没有同时显示多个不同授权码为当前有效码。
- [ ] 登录成功后仍需用户确认保存 Provider，不会在用户未保存时显示半套 Provider。

浏览器和返回应用的实际现象：

```text
<用户填写；不要填写网址中的敏感参数或授权码>
```

### 7.4 保存与重启

- [ ] 输入或确认非敏感 Provider 标签。
- [ ] 点击保存 Provider。
- [ ] 保存期间有明确状态，连续点击不会重复创建 Provider。
- [ ] 保存成功后 Provider 列表只出现一个预期实例。
- [ ] 关闭并重新启动应用。
- [ ] 重启后 Provider 仍存在，登录状态仍可用。
- [ ] UI 没有展示完整 Token 或内部凭据 JSON。

## 8. 取消、网络和生命周期

每个新场景都应重新发起一次登录并使用新授权码。不得在反馈中记录任何一次授权码。

### 8.1 取消后重新开始

- [ ] 在等待授权时点击取消。
- [ ] 取消后没有显示成认证失败。
- [ ] 取消后可以立即重新开始。
- [ ] 新尝试显示新的当前授权信息。
- [ ] 在旧网页中完成旧尝试不会覆盖新尝试或保存旧凭据。

如无法安全验证最后一项，填写：

```text
未执行旧网页迟到授权；原因：
```

### 8.2 临时断网

操作前确认不会影响手机上的紧急通信或其他重要任务。

- [ ] 在请求授权信息前断开测试网络，应用不崩溃。
- [ ] 应用不会永久加载，并显示明确的网络或代理提示。
- [ ] 恢复网络后可以重新发起登录。
- [ ] 在等待授权期间短暂断网，应用不崩溃。
- [ ] 连续网络失败后流程会明确停止，不会无限重试。
- [ ] 网络异常没有创建无凭据 Provider 或半套 Provider。

### 8.3 后台和锁屏

- [ ] 等待授权时切到后台，再返回应用。
- [ ] 返回后仍是同一当前尝试，没有重复请求或混淆授权码。
- [ ] 等待授权时锁屏，再解锁。
- [ ] 解锁后状态保持一致或明确结束，不会错误显示登录成功。

### 8.4 屏幕旋转

- [ ] 等待授权时旋转屏幕。
- [ ] 旋转后仍显示同一当前授权信息。
- [ ] 旋转没有重复发起网络请求。
- [ ] 授权完成后能正常保存，未出现重复 Provider。

### 8.5 Activity 重建和进程死亡

初次验收不要求开启“不要保留活动”、修改开发者选项或使用强制停止。只有用户与 Codex 另行确认安全步骤后才执行。

- [ ] 本轮不主动触发 Activity 重建。
- [ ] 本轮不主动触发进程死亡。
- [ ] 如系统自然重建 Activity，当前登录没有重复、覆盖或错误完成。
- [ ] 如系统彻底杀死进程，重新进入后没有恢复旧授权码，用户需要重新发起登录。

实际情况：

```text
<用户填写>
```

### 8.6 登录成功但保存前离页

- [ ] 完成网页授权，停留在尚未保存 Provider 的状态。
- [ ] 返回上一步或离开新增 Provider 页面。
- [ ] Provider 列表没有出现未确认保存的实例。
- [ ] 再次新增时需要重新发起登录，不会复用旧成功结果。

## 9. 模型、会话和刷新

### 9.1 模型准备

- [ ] Provider 保存后能看到预期模型或现有安全回退模型。
- [ ] 如果模型准备失败，Provider 和有效登录仍保留。
- [ ] 模型失败时有明确提示，并可单独重试。
- [ ] 重试模型不会要求重新执行设备登录。

如果无法安全制造模型加载失败：

```text
模型失败场景未主动制造；原因：
```

### 9.2 最小真实请求

只有第 3.2 节明确选择“允许执行一次最小真实请求”时才能继续。

- [ ] 用户再次确认当前账号、模型和请求可能产生用量。
- [ ] 选择已保存 OpenAI Provider 的一个模型。
- [ ] 新建测试会话。
- [ ] 发送一条不含个人信息、文件或敏感上下文的最小文本请求。
- [ ] 收到正常回复。
- [ ] 记录是否产生可见用量，但不记录账号余额、组织信息或账单截图。
- [ ] 用户本轮不同意执行真实模型请求。

若未执行，本轮只能结论为“核心登录和生命周期已验收，真实请求待验”，不能宣称 AC-01 全部完成。

### 9.3 Token 自动刷新

- [x] Phase 5 已提供模拟过期 Token 的自动刷新回归证据：见 Phase 5 报告第 4 节和
  `OpenAIDeviceTokenRefreshTest` 的 11 项结果。
- [ ] 本轮真机自然观察到刷新后仍可请求。
- [ ] 验收窗口内无法安全触发，记录为“自动化刷新验证通过，真机刷新待长期观察”。

不得通过修改系统时间、编辑加密存储、反复撤销账号授权或其他高风险方式强行触发刷新。

## 10. 兼容性与删除后新建策略

### 10.1 兼容性抽查

- [ ] 原浏览器回调入口仍可见。
- [ ] 在用户允许的测试账号上，原浏览器登录没有明显回归。
- [ ] API Key 新建流程仍可进入。
- [ ] 手动 Bearer Token 流程仍可进入。
- [ ] 已保存 OpenAI OAuth Provider 升级后仍存在。
- [ ] 其他 Provider、会话和工作区没有发现与本功能相关的回归。

未执行的兼容性项目及原因：

```text
<用户填写>
```

### 10.2 删除后重新新建

只允许删除本轮刚创建、确认没有日常模型配置或会话依赖的测试 Provider。

- [ ] 用户确认目标是可删除的测试 Provider。
- [ ] 删除测试 OpenAI OAuth Provider。
- [ ] 删除后 Provider 从列表消失。
- [ ] 重新新增时需要重新登录，不会直接复用已删除实例的登录状态。
- [ ] 如需改用设备码，按“删除旧 Provider 后新建”处理，不要求迁移旧模型配置。
- [ ] 本轮没有安全可删除的测试 Provider，因此不执行。

## 11. 脱敏日志和截图收集

### 11.1 默认原则

优先提供手工描述和脱敏截图。只有现象无法定位且用户同意时才收集日志。

日志或截图不得包含：

- 一次性授权码；
- 完整登录网址中的查询参数；
- device authorization ID；
- authorization code；
- Access Token、Refresh Token、ID Token；
- PKCE verifier、challenge 或 state；
- 完整 HTTP 请求或响应正文；
- 账号邮箱、手机号、workspace、账单或组织信息；
- 与本缺陷无关的聊天内容、文件路径或其他应用信息。

### 11.2 可选日志步骤

1. 先复现一次最小问题。
2. 将日志保存在用户本机，不直接上传原文件。
3. 逐行检查并删除上述敏感信息。
4. 只保留问题前后最短必要时间段。
5. 用 `<已删除授权码>`、`<已删除Token>` 等固定占位符替换敏感值。
6. 分享前再次检查截图缩略图、文件名和剪贴板。

用户已配置 ADB 且愿意使用时，可在复现后把当前缓冲区保存到本地：

```powershell
adb logcat -d -v threadtime | Out-File -Encoding utf8 -LiteralPath '.\openminis-device-login-local-review.txt'
```

该命令生成的是未经脱敏的本地原始日志。不得直接发送，必须先人工审查和脱敏。测试完成后由用户决定是否安全删除本地原始日志。

### 11.3 脱敏日志说明

| 项目 | 用户填写 |
|---|---|
| 是否收集日志 |  |
| 日志时间范围 |  |
| 已检查的敏感字段 |  |
| 是否只保留必要片段 |  |
| 是否确认可以分享 |  |

## 12. 立即停止条件

出现任一情况立即停止真机测试，不继续尝试登录、保存或真实请求：

- [ ] APK 文件、提交或 SHA-256 与声明不一致。
- [ ] 授权页面域名、证书或内容异常。
- [ ] 应用、UI、日志或错误提示泄漏授权码、Token、PKCE、完整响应或其他敏感信息。
- [ ] 出现未知账号、未知 workspace、异常授权范围或不可解释的账号行为。
- [ ] 旧登录任务覆盖新任务，或旧授权结果写入新 Provider。
- [ ] 取消、离页、超时或进程重建后仍保存了旧凭据。
- [ ] 登录成功但保存失败后留下无法管理的 Provider 或孤立登录状态。
- [ ] 应用要求卸载、清除数据、关闭系统安全能力或安装来源不明的软件才能继续。
- [ ] 真实请求可能产生用户未明确同意的费用或用量。
- [ ] 应用崩溃、永久加载或重复创建 Provider，且继续操作可能扩大数据影响。

停止时间、步骤和脱敏现象：

```text
<用户填写>
```

停止后：

1. 不重复输入授权码；
2. 不删除日常 Provider 或应用数据；
3. 保留 APK 身份、设备版本和最小脱敏现象；
4. 在第 13 节登记缺陷；
5. 由 Codex 判断责任 Phase，修复后重新完成 Phase 5，再提供新 APK 复测。

## 13. 用户反馈与缺陷记录

### 13.1 场景结果汇总

| 编号 | 场景 | 状态（通过/失败/未执行） | 实际结果或未执行原因 |
|---|---|---|---|
| 1 | APK 身份和安全升级 |  |  |
| 2 | 双登录入口和范围限制 |  |  |
| 3 | 获取、展示、打开和复制 |  |  |
| 4 | 正常授权和保存 |  |  |
| 5 | 取消和立即重试 |  |  |
| 6 | 临时断网和恢复 |  |  |
| 7 | 后台和锁屏 |  |  |
| 8 | 屏幕旋转 |  |  |
| 9 | Activity 重建/进程死亡 |  |  |
| 10 | 保存前离页清理 |  |  |
| 11 | 重启后 Provider 持久化 |  |  |
| 12 | 模型准备失败与重试 |  |  |
| 13 | 最小真实请求 |  |  |
| 14 | Token 自动刷新 |  |  |
| 15 | 现有登录方式兼容性 |  |  |
| 16 | 删除测试 Provider 后新建 |  |  |
| 17 | UI 和日志安全 |  |  |

### 13.2 单个缺陷模板

| 项目 | 用户填写 |
|---|---|
| 缺陷编号 | `D6-` |
| APK SHA-256 前 12 位 |  |
| 对应提交前 12 位 |  |
| 设备和 Android 版本 |  |
| 网络/代理概况 |  |
| 发生场景和步骤 |  |
| 预期结果 |  |
| 实际结果（已脱敏） |  |
| 是否可稳定复现 |  |
| 是否停止测试 |  |
| 是否有脱敏截图/日志 |  |
| 初步责任 Phase（由 Codex 填写） |  |
| 修复提交和新 APK |  |
| 复测结果 |  |

如有多个缺陷，复制本模板分别填写，不要在一个记录中混合多个根因。

## 14. Phase 6 验收结论

### 14.1 用户结论

- [ ] Phase 6 通过：核心登录、生命周期、持久化、兼容性和安全场景均符合预期，所有阻断缺陷已修复并复测。
- [ ] 部分通过：核心登录和生命周期已验收，但真实模型请求待验。
- [ ] 未通过：存在阻断缺陷，必须回到责任 Phase。
- [ ] 尚未开始或仍在执行。

真实模型请求：

- [ ] 已执行并成功
- [ ] 已执行但失败
- [ ] 用户选择不执行

Token 自动刷新：

- [ ] 自动化与真机均有证据
- [ ] 自动化通过，真机待长期观察
- [ ] 存在问题，已登记缺陷

用户最终意见：

```text
<用户填写>
```

用户确认日期：

```text
<用户填写>
```

### 14.2 当前默认结论

在用户完成并确认本清单前：

- Phase 6 保持“待用户验收”；
- 不得声称真实设备登录、系统浏览器、旋转、锁屏、应用重启或真实请求已经通过；
- Phase 7 的最终交付门禁尚未满足。

## 15. Phase 7 交付门禁预留

只有第 14 节由用户明确确认 Phase 6 通过后，才能填写以下项目：

- [ ] 所有阻断缺陷已经回到责任 Phase 修复。
- [ ] 修复后的 focused tests、完整基线对比和 Debug APK 构建重新通过 Phase 5。
- [ ] 用户接受的 APK 文件名、SHA-256 和提交已固化。
- [ ] 最终修改说明、文件清单、自动化结果、真机结果和未执行验证已经整理。
- [ ] 提交历史按 Phase 分组并使用详细中文说明。
- [ ] 工作区只包含预期修改，`git diff --check` 通过。
- [ ] 功能分支已经推送到个人远端 `origin`，远端提交与本地一致。
- [ ] 官方 `upstream` 的 push 仍被禁用。
- [ ] 没有向官方镜像创建 Pull Request。
- [ ] 文档不含真实账号、Token、授权码、PKCE 或其他敏感信息。
- [ ] APK、提交和对应源码可以相互验证。
- [ ] GPLv3 对应源码和许可证要求已满足。
- [ ] 后续可以按 `UPSTREAM_SYNC.md` 同步 `upstream/main` 并处理冲突。
- [ ] 已知限制、协议变更风险和真机待长期观察项均已记录。

Phase 7 当前状态：

```text
待 Phase 6 用户验收通过
```
